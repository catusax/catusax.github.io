image: node:alpine

before_script:
  - export TZ='Asia/Shanghai'
  - git config --global user.name "coolrc136"
  - git config --global user.email "renchao13619102485@gmail.com"
  - npm install -g hexo-cli
  - npm install

pages:
  cache:
    paths:
    - node_modules/

  script:
  - hexo deploy
  artifacts:
    paths:
    - public
  only:
  - master

